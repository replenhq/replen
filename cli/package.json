{
  "name": "replen",
  "version": "0.4.1",
  "private": true,
  "_forkNote": "Fork-only — never publish from this tree. The live replen CLI is published from nsokin/replen, not nsokin/replen-skill. 'private: true' makes 'npm publish' refuse from this directory until we explicitly rename + publish to a new package name. See FORK.md.",
  "description": "Smarter AI Development workflows. The AI that asks 'can we do this better?' - replen reads your codebase against the live ecosystem and surfaces drop-in libraries, ideas to port, and patterns to learn from. Calm cadence: 1-3 actionable matches a month. One-command setup: opens a browser to sign in, then wires the MCP server into Claude Code / Codex.",
  "type": "module",
  "bin": {
    "replen": "dist/index.js"
  },
  "scripts": {
    "build": "tsc && chmod +x dist/index.js",
    "dev": "tsx src/index.ts",
    "prepublishOnly": "npm run build"
  },
  "files": [
    "dist",
    "extras",
    "README.md",
    "LICENSE"
  ],
  "keywords": [
    "replen",
    "claude-code",
    "mcp",
    "cli"
  ],
  "homepage": "https://replen.dev",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/replenhq/replen.git",
    "directory": "cli"
  },
  "bugs": "https://github.com/replenhq/replen/issues",
  "author": "replen.dev",
  "license": "SEE LICENSE IN LICENSE",
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}

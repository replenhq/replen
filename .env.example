# ── Database ────────────────────────────────────────────────────────────
DIGEST_DB_PATH=./data/digest.sqlite

# ── At-rest encryption ──────────────────────────────────────────────────
# Symmetric key for AES-256-GCM encryption of user secrets (PATs, LLM keys
# stored in user_settings). Generate a fresh one with:
#   openssl rand -base64 32
ENCRYPTION_KEY=

# Optional: previous ENCRYPTION_KEY during a rotation window. When set, the
# server tries the current key first, then the previous key, so a half-
# rotated DB stays readable. Set this to the OLD value, flip ENCRYPTION_KEY
# to the NEW value, then run:
#   node --env-file=.env --import=tsx src/cli/rotate-master-key.ts
# Once the rotate CLI reports all rows on the current key, remove
# ENCRYPTION_KEY_PREV from this file and restart services. See M2 in the
# 2026-05-18 security audit.
ENCRYPTION_KEY_PREV=

# ── Auth ────────────────────────────────────────────────────────────────
# AUTH_MODE: 'firebase' for multi-user (signup/login flow); 'solo' for
# single-user self-hosters who skip Firebase entirely. Default: firebase.
AUTH_MODE=firebase

# Firebase Admin (only used when AUTH_MODE=firebase)
FIREBASE_PROJECT_ID=
FIREBASE_CLIENT_EMAIL=
FIREBASE_PRIVATE_KEY_BASE64=

# ── LLM providers (BYO keys; users can also set their own on /settings) ──
# Two slots, provider-agnostic. Wire any vendor by pointing the base URL +
# model at it. Per-user keys on /settings override these shared fallbacks.
#
# Primary slot - used for triage + low-sensitivity reasoning.
# OpenAI-compatible /chat/completions wire format. Works with DeepSeek,
# OpenAI, Groq, Together, Fireworks, OpenRouter, local llama.cpp / ollama,
# anything that speaks the OpenAI chat API.
LLM_PRIMARY_API_KEY=
LLM_PRIMARY_BASE_URL=https://api.deepseek.com
LLM_PRIMARY_MODEL=deepseek-v4-flash
TRIAGE_MODEL=deepseek-v4-flash
REASONING_MODEL=deepseek-v4-flash

# Sensitive slot - used for projects flagged high-sensitivity (skipped
# otherwise). Defaults to Anthropic's /v1/messages wire format; set
# LLM_SENSITIVE_WIRE_FORMAT=openai-compatible to route through
# /chat/completions instead (e.g. for a privately-hosted model).
LLM_SENSITIVE_API_KEY=
LLM_SENSITIVE_BASE_URL=https://api.anthropic.com
LLM_SENSITIVE_MODEL=claude-opus-4-7
LLM_SENSITIVE_WIRE_FORMAT=anthropic
REASONING_MODEL_HIGH=claude-opus-4-7

# Legacy aliases (DEEPSEEK_* / ANTHROPIC_*) still work if set.

# ── Semantic embeddings (REQUIRED for the matcher to actually rank) ──────
# The skill-tier matcher ranks candidates by cosine similarity between an
# OpenAI text-embedding-3-small vector of each project and each candidate
# (src/lib/embeddings.ts). This is SEPARATE from the LLM slots above and is
# NOT covered by DeepSeek/Anthropic keys — it must be an OpenAI (or
# OpenAI-compatible /v1/embeddings) key. OPENAI_EMBEDDING_KEY is accepted as
# an alias.
#
# If this is unset the embed calls log a warning and no-op SILENTLY: every
# project/candidate embedding stays null, cosine never computes, and the
# inventory degrades to the unfiltered firehose (same generic candidates for
# every repo). Set this in any deployment that wants real semantic matches.
OPENAI_API_KEY=

# ── GitHub (fallback; per-user PAT preferred) ───────────────────────────
GITHUB_TOKEN=
GITHUB_TOKEN=

# ── Fetcher sources (defaults; per-user overrides on /sources) ──────────
REDDIT_USER_AGENT=replen/0.1 (by /u/yourhandle)
REDDIT_SUBS=MachineLearning,LocalLLaMA,selfhosted,programming,opensource

# Threads via RSSHub - optional. Spin up RSSHub yourself or point at an
# instance you trust. https://docs.rsshub.app/install/
RSSHUB_BASE=http://127.0.0.1:1200
THREADS_HANDLES=

# ── Email delivery ──────────────────────────────────────────────────────
# Provider: 'resend' (recommended, 5-min setup) or 'ses' (AWS SES via SMTP).
EMAIL_PROVIDER=resend

# Resend (https://resend.com - get an API key + verify a domain)
RESEND_API_KEY=
EMAIL_FROM_ADDRESS=you@example.com
EMAIL_FROM_NAME=replen
EMAIL_TO_ADDRESS=

# Amazon SES SMTP (only used when EMAIL_PROVIDER=ses)
SES_SMTP_HOST=email-smtp.eu-west-2.amazonaws.com
SES_SMTP_PORT=587
SES_SMTP_USERNAME=
SES_SMTP_PASSWORD=

# ── Project profiles ────────────────────────────────────────────────────
# Where the digest service finds your local project repos to build
# per-project relevance profiles. Path on the host running this service.
GITHUB_ROOT=~/projects

# ── Sync CLI ────────────────────────────────────────────────────────────
# Random string; protects the /api/sync endpoint used by the laptop sync CLI.
SYNC_TOKEN=
# /api/sync is bound to exactly one user (the laptop owner). The numeric
# `users.id` of that user. Required; other user_id params return 403 even
# with the right SYNC_TOKEN.
SYNC_USER_ID=

# ── Public base URL ─────────────────────────────────────────────────────
# Used in email links, MCP setup commands, etc. For self-host this is your
# own domain (e.g. https://digest.your-domain.com); for local dev,
# http://localhost:3030.
PUBLIC_BASE_URL=http://localhost:3030

# Origin the CLI/MCP should call for the API. Kept SEPARATE from
# PUBLIC_BASE_URL on purpose: in prod PUBLIC_BASE_URL points at a gated
# marketing host (skill.replen.dev) the CLI can't reach, so the CLI auth flow
# must hand back the public app origin instead. Defaults to
# https://app.replen.dev when unset. Override only for self-host.
CLI_PUBLIC_BASE_URL=

# ── Skill-tier matcher: relevance floor + adaptive lookback ─────────────
# Relevance floor: a scoped-project candidate must clear this cosine bar to
# be surfaced. If nothing clears it the response is empty and the "By the
# way…" footnote stays silent — silence beats a weak match. text-embedding-
# 3-small cosines run low in absolute terms; 0.40 keeps genuinely-related
# repos (~0.45-0.60) and drops noise (~0.20-0.35). Tunable per deployment.
REPLEN_MIN_COSINE=0.48
# Adaptive lookback window (days). First-run is keyed PER-PROJECT: a project
# that's never surfaced a match the user engaged with (added today, never run,
# or dormant for weeks) gets the wide FIRST-RUN window so its first match sees
# months of inventory (the good per-project gh-targeted candidates are often
# days-to-weeks old). A project that's already surfaced matches gets the STEADY
# window. STEADY defaults to 30, not 7, because Replen's positioning is "1-3
# matches per MONTH" — a 7-day window systematically missed ~3 weeks of every
# month's candidates. An explicit ?days= on the request overrides both.
REPLEN_FIRSTRUN_DAYS=180
REPLEN_STEADY_DAYS=30
# Faceted matching (Phase 1). A project is embedded as a CENTROID plus one
# vector per capability (keyCapabilities). Candidates score on their BEST facet,
# so a library that fills ONE capability (a CV lib → "computer vision") surfaces
# even when it's far from the blended centroid. The centroid then drives
# competitor suppression: an APP whose centroid similarity clears
# REPLEN_COMPETITOR_CENTROID is "basically my whole project" (a competitor, not
# a component) and is dropped — UNLESS a specific facet beats the centroid by
# REPLEN_FACET_LEAD (it leads with a capability, so it fills a part) or it's a
# dependency match. Libraries/tools are never suppressed. Gated on the project
# having facet vectors, so pre-backfill projects are unaffected.
REPLEN_COMPETITOR_CENTROID=0.5
REPLEN_FACET_LEAD=0.04

# ── Cross-user learning loop (L4) ───────────────────────────────────────
# Global demote: a repo whose LATEST-verdict skip ratio crosses
# REPLEN_GLOBAL_SKIP_RATIO across at least REPLEN_GLOBAL_SKIP_MIN distinct
# users is suppressed for everyone (many people judged it rubbish).
REPLEN_GLOBAL_SKIP_MIN=3
REPLEN_GLOBAL_SKIP_RATIO=0.6
# Similar-project promote: a repo positively triaged (adopt/port) by users
# whose project is at least this cosine-similar to the caller's project is
# eligible to surface to the caller too. REPLEN_PROMOTE_MAX caps how many
# promotions augment a single response; REPLEN_PROMOTE_SCAN bounds how many
# recent positive triages are scanned per request.
REPLEN_SIMILAR_PROJECT_COSINE=0.6
REPLEN_PROMOTE_MAX=3
REPLEN_PROMOTE_SCAN=2000

# ── Pattern A — "watch your stack" (stack-watch fetcher) ────────────────
# Emits recent GitHub Releases of the vendors a project actually depends on
# (registry: src/fetchers/stack-watch/registry.ts). A release whose package
# is in the scoped project's deps is a dependency match — it bypasses the
# relevance floor and sorts to the top. REPLEN_STACK_WINDOW_DAYS bounds how
# far back a release counts as "recent"; REPLEN_STACK_MAX_PER_VENDOR caps
# releases per vendor per run. Uses GITHUB_TOKEN for the releases API.
REPLEN_STACK_WINDOW_DAYS=90
REPLEN_STACK_MAX_PER_VENDOR=3

# ── Pattern B — "watch what regulates your code" (spec-watch fetcher) ────
# Emits recent changes to the standards a project implements: EIPs/ERCs
# (web3 deps), TC39 proposals (JS/TS), Chrome Platform Status deprecations
# (frontend deps). Registry: src/fetchers/spec-watch/registry.ts. A change to
# a standard the scoped project has a stake in bypasses the relevance floor
# and leads the footnote. WINDOW_DAYS bounds recency; MAX_PER_SOURCE caps
# items per spec source per run. Uses GITHUB_TOKEN for the commit-backed
# sources (EIPs/ERCs/TC39); Chrome Status is a public JSON API.
REPLEN_SPEC_WINDOW_DAYS=30
REPLEN_SPEC_MAX_PER_SOURCE=5

# ── Pattern C — "watch the health of what you build on" (health-watch) ──
# Raises an alert when an upstream a project depends on looks risky:
#   C11 dep health  — a direct dep gone stale/dead/archived (probeDepHealth;
#                     thresholds PRUNE_STALE_DAYS / PRUNE_DEAD_DAYS above)
#   C12 issues      — a high-engagement open issue others are hitting in a dep
#   C13 status      — an active incident on a managed service you use
#                     (status-registry.ts; standard Statuspage API)
# C14 (vendor outage <-> YOUR error rates) needs error-log ingestion Replen
# doesn't have yet — a separate pipeline, intentionally not stubbed.
REPLEN_HEALTH_MAX_DEPS=30
REPLEN_HEALTH_ISSUE_REPOS=6
REPLEN_HEALTH_ISSUE_REACTIONS=10
REPLEN_HEALTH_ISSUE_WINDOW_DAYS=60

# ── Pattern C (security) — "watch for vulnerabilities in what you depend on" ──
# Maps each project's direct deps to known security advisories via osv.dev (the
# OSS Vulnerability DB — free, no auth). A recent advisory on a dep you use is a
# stake match: it bypasses the relevance floor and leads the footnote ("a
# security advisory affects a dependency you use"). Coarse match: by package
# name + ecosystem, not exact installed version (refining to affected ranges
# needs the lockfile, a follow-up). MIN_SEVERITY gates noise — default HIGH so
# only advisories you'd act on surface (LOW|MODERATE|HIGH|CRITICAL). WINDOW_DAYS
# bounds recency by advisory publish date; MAX caps surfaced advisories per run.
REPLEN_SECURITY_WINDOW_DAYS=180
REPLEN_SECURITY_MIN_SEVERITY=HIGH
REPLEN_SECURITY_MAX=12
REPLEN_SECURITY_MAX_DEPS=60

# ── Stage 4 source-context verification (experimental, off by default) ──
# Set to "1" to enable BM25 source verification for gh-targeted candidates
# that the cheap README-only pass scores as medium/high. When on, those
# candidates are shallow-cloned, indexed, and re-scored with the top source
# excerpts injected into the prompt — catches the case where a sales-pitch
# README claims functionality the code doesn't support. Adds ~30-70s per
# verified candidate (one-time clone+index, cached against readme_sha).
# Off in prod for now: current attributions are all inferred/medium which
# the LLM already strict-biases to general-awareness, so verification has
# nothing to correct yet.
STAGE4_VERIFY_WITH_SOURCE=0

# ── Discovery caps (Stage 5) ──
# How many "discovered" (broad-net: HN, reddit, gh-trending) candidates per
# user per run survive into the LLM batch. Targeted/scouted candidates aren't
# capped here — they're already filtered by outcome attribution.
DISCOVERED_CANDIDATE_CAP=15
# Of the matches the LLM produces from the discovered pool, keep the top N by
# relevance score and prune the rest. -1 disables.
DISCOVERED_MATCH_CAP=3
# Back-compat: SERENDIPITY_CANDIDATE_CAP / SERENDIPITY_MATCH_CAP are still
# read as fallbacks when DISCOVERED_* is unset.

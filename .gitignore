# Build artefacts
node_modules
.next
*.tsbuildinfo
dist
build

# Local state - never commit
.env
.env.local
.env.*.local
data/
projects-mirror/
backups/

# Logs
*.log

# OS / editor
.DS_Store
.idea
.vscode

# Claude Code session files
.mcp.json

# Operator-specific deploy wrappers (private hostnames/domains)
scripts/*.local.sh
scripts/*.local.plist

# Local operator notes (deployment topology, credentials, runbooks).
# Kept out of the public mirror.
*.local.md

# Synthetic test-cohort harness (operator-only; cohort.json holds real tokens)
testbed/
